Realtime Database rules (add these into Firebase console or use firebase deploy --only database:rules)

{
  "rules": {
    ".read": "now < 1750474800000",
    ".write": "now < 1750474800000",
    "users": {
      ".indexOn": ["email"]
    }
  }
}

Note: these rules allow read/write until the timestamp 1750474800000. Adjust as needed for production.
